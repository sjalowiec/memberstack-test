/**
 * Temporary launch hold while machine shipping/ordering is being finalized.
 * Set to `false` to restore the full /shop/machines catalog and product pages.
 */
export const MACHINE_SALES_HOLD = true;
