---
export const prerender = true;

import BaseLayout from "../layouts/BaseLayout.astro";
import TransitionPage from "../components/transition/TransitionPage.astro";
import { description, pageTitle } from "../components/transition/transitionPageData";
---

<BaseLayout
  title={`${pageTitle} | Knit it Now`}
  {description}
  skipGlobalHyvor={true}
  skipGlobalPinterest={true}
  bodyClass="transition-page-body"
>
  <TransitionPage contactHref="/contact/" />
</BaseLayout>
