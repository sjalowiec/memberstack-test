---
import Layout from "../../../layouts/Layout.astro";
import EWrapCastOnSkillBuilder from "../../../components/skill-builders/EWrapCastOnSkillBuilder.astro";
import { getEWrapCastOnSkillBuilder } from "../../../lib/skillBuilders/eWrapCastOnSkillBuilder";

export const prerender = true;

const builder = getEWrapCastOnSkillBuilder();
---

<Layout
  title={`${builder.title} | Skill Builders | Knit it Now`}
  description={builder.intro}
  bodyClass="page--tight-header"
>
  <EWrapCastOnSkillBuilder />
</Layout>
