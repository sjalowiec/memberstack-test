---
/**
 * Retired: KIN beta signup previously assigned pln_kin-beta-access-vyek0a38 via
 * data-ms-plan:add. Redirect to membership plans so the plan can no longer be
 * granted from this route.
 */
return Astro.redirect("/membership", 301);
---
