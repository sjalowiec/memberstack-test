---
/**
 * Legacy review/customize URL — redirects to the pattern workspace (Phase 2).
 * Review UI and scripts remain in the repo for reference; this route no longer renders them.
 */
import { OPEN_PATTERN_HREF } from "../../../lib/patterns/customPatternProjectNavigation";
import { buildPatternReviewLegacyRedirect } from "../../../lib/patterns/patternReviewLegacyRedirect";

return Astro.redirect(buildPatternReviewLegacyRedirect(Astro.request.url, OPEN_PATTERN_HREF));
---
