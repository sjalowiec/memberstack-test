---
/**
 * Legacy Express builder URL — permanently redirects to the unified builder.
 * Preserves query params (`?new=1`, `?edit=choices`, etc.).
 */
const target = new URL(Astro.url);
target.pathname = "/patterns/sleeveless/builder";
return Astro.redirect(`${target.pathname}${target.search}`, 308);
---
