---
/**
 * Legacy all-in-one Hat wizard URL — permanently redirects to the current Hat Builder.
 * Preserves query params so old bookmarks and links keep working.
 */
import { buildHatLegacyEntryRedirect } from "../../lib/patterns/hat/hatPatternNavigation";

return Astro.redirect(buildHatLegacyEntryRedirect(Astro.url.href), 308);
---
