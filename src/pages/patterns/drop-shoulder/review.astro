---
/**
 * Legacy drop-shoulder review URL — redirects to the pattern workspace (Phase 2).
 */
import { DROP_SHOULDER_OPEN_PATTERN_HREF } from "../../../lib/patterns/customPatternProjectNavigation";
import { buildPatternReviewLegacyRedirect } from "../../../lib/patterns/patternReviewLegacyRedirect";

return Astro.redirect(
  buildPatternReviewLegacyRedirect(Astro.request.url, DROP_SHOULDER_OPEN_PATTERN_HREF),
);
---
